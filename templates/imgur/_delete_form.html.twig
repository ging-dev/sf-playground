<form method="post" action="{{ path('app_imgur_delete', {'id': imgur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imgur.id) }}">
    <button class="btn">Delete</button>
</form>
